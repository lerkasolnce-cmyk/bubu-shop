# bu-bu Shop Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Интернет-магазин колясок bu-bu (Одесса): каталог 2000–3000 товаров, корзина, monopay, Telegram-уведомления, админ-панель.

**Architecture:** Next.js 15 App Router (TS) на Vercel; Supabase = Postgres (products/categories/orders, RLS) + Storage (фото) + Auth (админ). Публичное чтение каталога напрямую из Supabase (server components), запись заказов и оплата — через server routes с service-ключом. Корзина в localStorage. Локали UA (дефолт)/RU через cookie.

**Tech Stack:** next@15, react@19, tailwindcss@4, @supabase/supabase-js, @supabase/ssr, zod, papaparse (CSV), vitest (unit-тесты логики).

## Global Constraints

- Рабочая папка проекта: `C:\Users\lerka\Removal\bubu-shop` (git-репозиторий, коммит после каждой задачи).
- Windows: node/npm глобально доступны; для скриптов использовать `npx tsx`, пути с прямыми слэшами.
- Секреты (`MONOBANK_TOKEN`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `SUPABASE_SERVICE_ROLE_KEY`) — только в `.env.local` / Vercel env; в клиентский бандл попадают только `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
- Сумма заказа ВСЕГДА пересчитывается на сервере по ценам из БД.
- Все пользовательские тексты — через словарь `t()` (UA/RU), товарные поля — колонки `*_ua` / `*_ru`.
- Дизайн-токены (Tailwind theme): cream `#FBF7F0`, blush `#F5D7D7`, mint `#CDE8DD`, ink `#4A3B32`, accent `#E8927C`; радиусы 16–24px, шрифт Nunito (Google Fonts, subset latin+cyrillic).
- Валюта: грн (₴), цены в БД в копейках НЕ храним — integer грн (коляски, копейки не нужны).
- Изображения товаров: Supabase Storage bucket `products` (public read), в `products.images` — массив полных URL.

## Структура файлов (карта)

```
bubu-shop/
  docs/superpowers/{specs,plans}/
  supabase/migrations/001_schema.sql
  scripts/seed.ts                     # стартовые категории + ~50 товаров
  src/
    lib/supabase/{client.ts,server.ts,admin.ts}   # anon browser / anon SSR / service-role
    lib/i18n/{dict.ua.json,dict.ru.json,index.ts} # getLocale(), t()
    lib/types.ts                      # Product, Category, Order, CartItem
    lib/cart.ts                       # чистые функции корзины (тестируемые)
    lib/mono.ts                       # createInvoice(), verifyWebhookSignature()
    lib/telegram.ts                   # sendOrderNotification()
    lib/csv.ts                        # parseProductsCsv(), toCsv() (тестируемые)
    components/                       # Header, Footer, ProductCard, Filters, CartDrawer...
    app/
      layout.tsx, page.tsx            # главная
      catalog/[slug]/page.tsx         # категория с фильтрами/пагинацией
      search/page.tsx
      product/[slug]/page.tsx
      cart/page.tsx
      checkout/page.tsx
      (static)/{delivery,about,contacts}/page.tsx
      api/orders/route.ts             # POST заказ (+ mono invoice при онлайн-оплате)
      api/mono-webhook/route.ts
      api/locale/route.ts             # POST переключение языка (cookie)
      admin/{layout.tsx,page.tsx,login/page.tsx}
      admin/products/{page.tsx,new/page.tsx,[id]/page.tsx}
      admin/categories/page.tsx
      admin/orders/{page.tsx,[id]/page.tsx}
      admin/import/page.tsx
    middleware.ts                     # guard /admin
  tests/{cart.test.ts,csv.test.ts,mono.test.ts}
```

---

### Task 1: Каркас проекта

**Files:** create-next-app в `bubu-shop`, правки `src/app/layout.tsx`, `globals.css`, `tailwind` токены, `.env.example`, `.gitignore`.

**Interfaces:** Produces: рабочий dev-сервер, дизайн-токены (цвета/шрифт из Global Constraints) как CSS-переменные Tailwind.

- [ ] `npx create-next-app@latest bubu-shop --ts --tailwind --app --src-dir --no-eslint --use-npm` (в `C:\Users\lerka\Removal`; папка уже содержит docs — при конфликте scaffold во временную папку и перенести).
- [ ] `npm i @supabase/supabase-js @supabase/ssr zod papaparse && npm i -D vitest tsx @types/papaparse`
- [ ] `globals.css`: `@theme` с токенами (cream/blush/mint/ink/accent, radius), подключить Nunito через `next/font/google` в `layout.tsx` (`subsets: ['latin','cyrillic']`).
- [ ] `.env.example` со всеми ключами из Global Constraints; `.env.local` в `.gitignore` (проверить).
- [ ] Проверка: `npm run dev` → пустая главная со шрифтом Nunito и фоном cream.
- [ ] `git init && git add -A && git commit -m "chore: scaffold next.js app with design tokens"`

### Task 2: Схема БД, клиенты Supabase, seed

**Files:** Create: `supabase/migrations/001_schema.sql`, `src/lib/supabase/{client,server,admin}.ts`, `src/lib/types.ts`, `scripts/seed.ts`.

**Interfaces:** Produces: типы `Product`, `Category`, `Order`, `CartItem {slug, qty}`; `createBrowserClient()`, `createServerClient()` (anon, cookies), `createAdminClient()` (service key, только сервер).

- [ ] `001_schema.sql` — полностью:

```sql
create table categories (
  id uuid primary key default gen_random_uuid(),
  slug text unique not null,
  name_ua text not null, name_ru text not null,
  parent_id uuid references categories(id) on delete set null,
  sort int default 0
);
create table products (
  id uuid primary key default gen_random_uuid(),
  slug text unique not null,
  name_ua text not null, name_ru text not null,
  description_ua text default '', description_ru text default '',
  brand text not null,
  category_id uuid references categories(id) on delete set null,
  price int not null check (price >= 0),
  old_price int,
  stock_status text not null default 'in_stock'
    check (stock_status in ('in_stock','preorder','out_of_stock')),
  specs jsonb default '{}',
  images text[] default '{}',
  is_hit boolean default false, is_sale boolean default false,
  created_at timestamptz default now()
);
create index on products (category_id);
create index on products (brand);
create index on products (price);
create index on products (is_hit) where is_hit;
create extension if not exists pg_trgm;
create index products_name_trgm on products using gin ((name_ua || ' ' || name_ru) gin_trgm_ops);
create table orders (
  id bigint generated always as identity primary key,
  created_at timestamptz default now(),
  name text not null, phone text not null,
  city text not null, np_office text default '', comment text default '',
  items jsonb not null,          -- [{slug,name,price,qty}] снапшот на момент заказа
  total int not null,
  payment_method text not null check (payment_method in ('mono','cod')),
  payment_status text not null default 'pending'
    check (payment_status in ('pending','paid','failed','n/a')),
  order_status text not null default 'new'
    check (order_status in ('new','confirmed','shipped','done','cancelled')),
  mono_invoice_id text
);
alter table categories enable row level security;
alter table products enable row level security;
alter table orders enable row level security;
create policy "public read categories" on categories for select using (true);
create policy "public read products" on products for select using (true);
create policy "admin write categories" on categories for all
  using (auth.role() = 'authenticated') with check (auth.role() = 'authenticated');
create policy "admin write products" on products for all
  using (auth.role() = 'authenticated') with check (auth.role() = 'authenticated');
create policy "admin read orders" on orders for select using (auth.role() = 'authenticated');
create policy "admin update orders" on orders for update using (auth.role() = 'authenticated');
-- insert заказов только через service key (API route), публичной insert-политики нет
insert into storage.buckets (id, name, public) values ('products','products', true);
create policy "public read product images" on storage.objects for select using (bucket_id = 'products');
create policy "admin upload product images" on storage.objects for insert
  with check (bucket_id = 'products' and auth.role() = 'authenticated');
create policy "admin delete product images" on storage.objects for delete
  using (bucket_id = 'products' and auth.role() = 'authenticated');
```

- [ ] Клиенты: `client.ts` — `createBrowserClient` из `@supabase/ssr`; `server.ts` — `createServerClient` с `cookies()` из `next/headers`; `admin.ts` — `createClient(URL, SERVICE_ROLE_KEY, {auth:{persistSession:false}})` + `import 'server-only'`.
- [ ] `scripts/seed.ts`: категории — «Коляски 2в1», «Коляски 3в1», «Прогулочные», «Автокресла», «Аксессуары» (slug: `strollers-2in1`, `strollers-3in1`, `strollers-buggy`, `car-seats`, `accessories`); ~50 товаров: реальные модели Anex (e/type, m/type, Air-X, IQ), Cybex (Priam, Mios, Balios S, Eezy S, Cloud T, Sirona), Espiro (Next, Only, Sonic, Wave) с ориентировочными ценами в грн, `images: []` (фото добавит админ), у ~10 `is_hit`, у ~8 `is_sale` + `old_price`. Запуск: `npx tsx scripts/seed.ts` (использует admin-клиент, идемпотентно через upsert по slug).
- [ ] Инструкция для Валерии в `README.md`: создать проект на supabase.com → SQL Editor → вставить миграцию → скопировать URL/ключи в `.env.local`. (Если ключи уже даны — выполнить самим и запустить seed.)
- [ ] Проверка: `npx tsx scripts/seed.ts` → «seeded 5 categories, 50 products» (после получения ключей; до этого задача считается done по коду).
- [ ] Commit: `feat: db schema, supabase clients, seed data`

### Task 3: i18n + каркас Layout (Header/Footer)

**Files:** Create: `src/lib/i18n/*`, `src/components/{Header,Footer,LangSwitch,CartBadge}.tsx`, `src/app/api/locale/route.ts`. Modify: `src/app/layout.tsx`.

**Interfaces:** Produces: `getLocale(): Promise<'ua'|'ru'>` (cookie `locale`, дефолт `ua`); `getT(locale)` → `t(key)`; поле-хелпер `pick(row, 'name', locale)` → `row.name_ua|name_ru`. `CartBadge` читает localStorage `bubu_cart`.

- [ ] Словари: все строки UI обеих локалей сразу (шапка, футер, кнопки, корзина, checkout, статусы). Ключи плоские: `"header.search"`, `"cart.addToCart"` и т.д.
- [ ] `api/locale/route.ts`: POST `{locale}` → set cookie (httpOnly:false, maxAge 1 год) → клиент делает `router.refresh()`.
- [ ] Header: логотип «bu-bu» (текстовый, скруглённый бейдж blush), поиск (form → `/search?q=`), телефон, LangSwitch UA/RU, ссылка-корзина с бейджем количества; под ним полоса категорий (из БД). Мобильная версия: бургер-меню.
- [ ] Footer: контакты, Instagram `@bubu_odessa`, ссылки Доставка/О нас/Контакты.
- [ ] Проверка в preview: переключение UA/RU меняет тексты, меню категорий из БД.
- [ ] Commit: `feat: i18n, header/footer layout`

### Task 4: Главная страница

**Files:** Create: `src/app/page.tsx`, `src/components/{Hero,CategoryGrid,ProductCard,ProductRow,BrandStrip,Advantages}.tsx`.

**Interfaces:** Produces: `ProductCard({product, locale})` — переиспользуется в каталоге/поиске/похожих: фото (или плейсхолдер-иллюстрация коляски), бейджи «Хіт»/«Акція», название, цена + зачёркнутая old_price, статус наличия, кнопка «В корзину».

- [ ] Hero: пастельный баннер с крупным заголовком («Коляски для найменших — з любов'ю з Одеси»), CTA в каталог; мягкие «пузырьковые» декоративные круги CSS.
- [ ] CategoryGrid: карточки категорий (эмодзи/SVG-иконка + название + счётчик товаров).
- [ ] ProductRow «Хіти продажу» (`is_hit`, limit 8) и «Акції» (`is_sale`, limit 8) — горизонтальный скролл на мобиле, сетка на десктопе.
- [ ] BrandStrip: Anex / Cybex / Espiro текстовыми лого-плашками; Advantages: 4 пункта (доставка НП по Україні, гарантія, магазин в Одесі, оплата онлайн/при отриманні).
- [ ] Проверка: preview главной, мобильная ширина 375px без горизонтального скролла.
- [ ] Commit: `feat: home page`

### Task 5: Каталог, фильтры, поиск

**Files:** Create: `src/app/catalog/[slug]/page.tsx`, `src/app/search/page.tsx`, `src/components/{Filters,SortSelect,Pagination}.tsx`, `src/lib/catalog.ts`.

**Interfaces:** Produces: `fetchProducts({categoryId?, q?, brands?, minPrice?, maxPrice?, inStockOnly?, sort, page}): {items, total}` — единая функция для каталога и поиска; PAGE_SIZE=24; sort: `new|price_asc|price_desc|hits`.

- [ ] Всё состояние фильтров — в searchParams (`?brand=anex,cybex&min=5000&max=30000&sort=price_asc&page=2`), серверный рендер; `fetchProducts` строит запрос Supabase (`.eq/.in/.gte/.lte/.ilike`, `count:'exact'`, `.range()`).
- [ ] Filters (client component): чекбоксы брендов (distinct из выборки категории), два input цены, «тільки в наявності»; на мобиле — выезжающая панель. Изменение → router.push с новыми params.
- [ ] `search/page.tsx`: тот же грид, `q` → `.or('name_ua.ilike.%q%,name_ru.ilike.%q%')`.
- [ ] Пагинация: страницы + prev/next, сохраняет остальные params.
- [ ] Проверка: фильтр по бренду+цене сужает выдачу, пагинация листает, поиск «priam» находит Cybex Priam.
- [ ] Commit: `feat: catalog with filters, search, pagination`

### Task 6: Карточка товара

**Files:** Create: `src/app/product/[slug]/page.tsx`, `src/components/{Gallery,SpecsTable,AddToCart,SimilarProducts}.tsx`.

- [ ] Галерея: главное фото + миниатюры (клиентский стейт), плейсхолдер если фото нет.
- [ ] Блок покупки: цена/old_price, наличие, `AddToCart` (кнопки «В корзину» и «Купити зараз» → добавляет и redirect на `/cart`).
- [ ] Описание (локализованное) + SpecsTable из `specs` jsonb.
- [ ] SimilarProducts: 4 товара той же категории (не текущий).
- [ ] `generateMetadata`: title/description из товара.
- [ ] Проверка: открыть 2-3 seed-товара в preview.
- [ ] Commit: `feat: product page`

### Task 7: Корзина

**Files:** Create: `src/lib/cart.ts`, `src/components/CartProvider.tsx`, `src/app/cart/page.tsx`, `tests/cart.test.ts`.

**Interfaces:** Produces: чистые функции `addItem(items, slug, qty)`, `removeItem`, `setQty`, `cartTotal(items, products)`; `useCart()` контекст {items, add, remove, setQty, count} с sync в localStorage `bubu_cart` (формат `[{slug, qty}]`).

- [ ] Написать `tests/cart.test.ts` (add дублирует → qty++, setQty(0) удаляет, total считает по ценам товаров) → `npx vitest run` FAIL → реализовать `cart.ts` → PASS.
- [ ] CartProvider в `layout.tsx`; Header-бейдж из контекста.
- [ ] `/cart`: строки товаров (фото, имя, цена, qty +/-, удалить), итог, кнопка «Оформити замовлення» → `/checkout`; пустая корзина — милая заглушка с CTA в каталог.
- [ ] Проверка: добавить 2 товара, изменить qty, перезагрузить страницу (persist), итог верный.
- [ ] Commit: `feat: cart with localStorage persistence + tests`

### Task 8: Checkout + заказы + Telegram

**Files:** Create: `src/app/checkout/page.tsx`, `src/app/api/orders/route.ts`, `src/lib/telegram.ts`, `src/app/order/[id]/thanks/page.tsx`.

**Interfaces:** Produces: `POST /api/orders` body `{customer:{name,phone,city,npOffice,comment}, items:[{slug,qty}], paymentMethod:'mono'|'cod'}` → `{orderId, payUrl?}`. `sendOrderNotification(order)` — Telegram `sendMessage` (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`), ошибки глотает (заказ важнее уведомления).

- [ ] Форма (client): имя, телефон (маска +380), город, отделение НП, комментарий, радио способа оплаты; zod-валидация на клиенте и в route.
- [ ] Route: загрузить товары по slug через admin-клиент → пересчитать total по ценам БД → insert в `orders` (items = снапшот с именами и ценами) → Telegram → если `mono` — пока вернуть `payUrl: null` (подключим в Task 9), иначе redirect-данные на `/order/[id]/thanks`.
- [ ] Thanks-страница: номер заказа, «мы перезвоним», ссылка на Instagram.
- [ ] Проверка: оформить заказ (cod) в preview → строка в `orders`, сообщение в Telegram (если токены уже есть; иначе лог «telegram skipped»).
- [ ] Commit: `feat: checkout, orders api, telegram notifications`

### Task 9: Оплата monopay

**Files:** Create: `src/lib/mono.ts`, `src/app/api/mono-webhook/route.ts`, `tests/mono.test.ts`. Modify: `api/orders/route.ts`, thanks-страница (ветка «оплачено/ожидает»).

**Interfaces:** Produces: `createInvoice({amountUah, orderId, redirectUrl, webhookUrl}) → {invoiceId, pageUrl}` (POST `https://api.monobank.ua/api/merchant/invoice/create`, header `X-Token`, amount в копейках = грн*100, `merchantPaymInfo.reference = String(orderId)`); `verifyWebhookSignature(rawBody, xSignBase64, publicKeyBase64): boolean` (ECDSA SHA-256, `crypto.verify`).

- [ ] `tests/mono.test.ts`: verifyWebhookSignature на самосгенерированной ключевой паре (valid → true, подмена тела → false) → FAIL → реализовать → PASS.
- [ ] В orders-route: при `mono` → createInvoice → сохранить `mono_invoice_id` → вернуть `payUrl` (клиент делает `location.href = payUrl`).
- [ ] Webhook: raw body, проверить X-Sign (pubkey из env `MONO_PUB_KEY`, получить GET `/api/merchant/pubkey` один раз при настройке), статус `success` → `payment_status='paid'` по `invoiceId`; идемпотентно.
- [ ] Проверка: unit-тесты зелёные; сквозная оплата — после получения реального токена (отметить в README «проверить на проде»).
- [ ] Commit: `feat: monopay invoice + signed webhook`

### Task 10: Админка — вход и каркас

**Files:** Create: `src/middleware.ts`, `src/app/admin/{layout.tsx,page.tsx,login/page.tsx}`, `src/lib/supabase/middleware.ts`.

**Interfaces:** Produces: `/admin/*` требует Supabase-сессию (redirect на `/admin/login`); admin layout: сайдбар (Товари, Категорії, Замовлення, Імпорт, Вийти); dashboard: счётчики товаров/новых заказов.

- [ ] Middleware по гайду `@supabase/ssr` (updateSession), matcher `['/admin/:path*']`, login-страница исключена.
- [ ] Login: email+password `signInWithPassword`; админ-юзер создаётся в Supabase Dashboard руками (записать в README).
- [ ] Проверка: без сессии `/admin` → redirect; после логина — dashboard.
- [ ] Commit: `feat: admin auth and shell`

### Task 11: Админка — товары (CRUD + фото)

**Files:** Create: `src/app/admin/products/{page.tsx,new/page.tsx,[id]/page.tsx}`, `src/components/admin/{ProductForm,ImageUploader,SpecsEditor}.tsx`, `src/app/admin/products/actions.ts` (server actions).

**Interfaces:** Produces: server actions `saveProduct(formData)` (insert/update, slug автогенерация из name_ua транслитом при пустом), `deleteProduct(id)`. ImageUploader: upload в bucket `products` путём `{productSlug}/{timestamp}.{ext}` браузерным клиентом (сессия админа), превью, удаление, порядок.

- [ ] Список: таблица с поиском, фильтром категории, пагинацией (50/стр); колонки: фото-мини, название, бренд, цена, наличие, хит/акция; кнопки редактировать/удалить (confirm).
- [ ] Форма: все поля из схемы; SpecsEditor — пары ключ-значение; чекбоксы is_hit/is_sale; валидация zod.
- [ ] Проверка: создать товар с фото → виден на витрине; изменить цену → обновилась; удалить → пропал.
- [ ] Commit: `feat: admin product CRUD with image upload`

### Task 12: Админка — категории и заказы

**Files:** Create: `src/app/admin/categories/page.tsx` (+actions), `src/app/admin/orders/{page.tsx,[id]/page.tsx}` (+actions).

- [ ] Категории: список с inline-редактированием (name_ua/name_ru/slug/parent/sort), добавить/удалить (удаление — только если нет товаров, иначе alert).
- [ ] Заказы: таблица (номер, дата, имя, телефон, сумма, оплата, статус) с фильтром по статусу; деталь: состав, данные клиента, select статуса (`new→confirmed→shipped→done/cancelled`) — server action update.
- [ ] Проверка: сменить статус тестового заказа из Task 8.
- [ ] Commit: `feat: admin categories and orders`

### Task 13: CSV импорт/экспорт

**Files:** Create: `src/lib/csv.ts`, `src/app/admin/import/page.tsx` (+action), `tests/csv.test.ts`, `docs/import-template.csv`.

**Interfaces:** Produces: `parseProductsCsv(text): {rows: ProductCsvRow[], errors: string[]}`; колонки: `slug,name_ua,name_ru,brand,category_slug,price,old_price,stock_status,is_hit,is_sale,description_ua,description_ru`; upsert по slug батчами по 500.

- [ ] `tests/csv.test.ts`: валидный CSV → rows; кривая цена/неизвестная категория → в errors с номером строки; UTF-8 BOM обрабатывается → FAIL → реализовать (papaparse) → PASS.
- [ ] Страница: file input → предпросмотр первых 20 строк + счётчик/ошибки → кнопка «Імпортувати» → server action upsert → отчёт (создано/обновлено/пропущено). Кнопка «Експорт CSV» — выгрузка всех товаров тем же форматом. Ссылка на шаблон.
- [ ] Проверка: импорт файла с 3 новыми + 1 изменённым товаром.
- [ ] Commit: `feat: csv import/export`

### Task 14: Статические страницы, SEO, полировка

**Files:** Create: `src/app/(static)/{delivery,about,contacts}/page.tsx`, `src/app/{sitemap.ts,robots.ts,not-found.tsx}`. Modify: `layout.tsx` (metadata).

- [ ] Тексты страниц UA/RU (доставка НП, оплата mono/наложка, о магазине, контакты: Instagram, телефон-плейсхолдер `+380 __ ___ __ __` — Валерия заменит).
- [ ] `generateMetadata` везде; OpenGraph; `sitemap.ts` из БД (товары+категории); favicon-эмодзи 🍼 или простой SVG «b».
- [ ] Прогон: `npm run build` без ошибок; Lighthouse-переменные: images через `next/image` с remotePatterns supabase.
- [ ] Полировка UI по witness-чеклисту: 375px/768px/1280px, пустые состояния, длинные названия товаров.
- [ ] Commit: `feat: static pages, seo, polish`

### Task 15: Деплой

**Files:** Create: `README.md` (раздел «Запуск и деплой», чеклист для Валерии).

- [ ] Push в GitHub (private repo `bubu-shop`), импорт в Vercel, env-переменные, домен (пока `*.vercel.app`).
- [ ] Прод-проверка: заказ cod end-to-end + Telegram; monopay — после токена (webhook URL прописать в orders-route как `https://<domain>/api/mono-webhook`).
- [ ] README-чеклист: Supabase ключи, создание админ-юзера, mono-токен, Telegram-бот (@BotFather → токен; chat_id через @userinfobot), как править товары.
- [ ] Commit: `docs: deployment guide`

## Self-Review

- Покрытие спеки: витрина (T3-7,14), оплата (T9), админка (T10-12), CSV (T13), i18n (T3), seed (T2), деплой (T15) — все разделы закрыты.
- Типы согласованы: `CartItem {slug,qty}` (T7) = формат `items` в `POST /api/orders` (T8); `fetchProducts` один для каталога/поиска (T5); статусы заказов в схеме (T2) = селект в админке (T12).
- Тесты кода с логикой: cart, csv, mono-подпись; UI — проверка через preview.
